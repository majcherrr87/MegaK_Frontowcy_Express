<h3>Selected parts of cookie</h3>
<ul>

  <li>Base: {{cookie.base}} - {{pricify (findPrice bases cookie.base)}} PLN</li>
  {{#each cookie.addons}}
    <li>Addon:
      {{this}}
      -
      {{pricify (findPrice @root.addons this)}}
      PLN
      {{log noDeleteBtn}}
      {{#if noDeleteBtn}}
        <a
          class="btn btn-small"
          href="/configurator/delete-addon/{{this}}"
        >Delete</a>
      {{/if}}
    </li>
  {{/each}}

</ul>

<p><strong>Total of {{pricify sum}} PLN</strong></p>